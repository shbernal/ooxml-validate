// The package's public face.
//
// The validation API (`validate`, `validateBuffer`, `validateBuffers`,
// `validatorAvailable`, `FILE_FORMATS`) and the report types land here as they
// are implemented.

export {PACKAGE_NAME, PACKAGE_VERSION, RELEASE_TAG} from './version.ts';
